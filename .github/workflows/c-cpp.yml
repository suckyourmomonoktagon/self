name: C/C++ CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: No root autotools build in this repository
        run: echo "Skipping root C/C++ autotools build. Native/mobile code is validated by dedicated workflows."
